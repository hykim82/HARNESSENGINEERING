{
  "header": "cycle 0: no transitions, all PRIMARY_CLAUDE",
  "entries": [],
  "note": "HYK-167 사이클0 D0-3 (B0 §5.3/§4.2): this repo's real migration ledger, validated by migration-ledger.schema.mjs's validateMigrationLedger. Cycle 0 makes zero legacy_hook_state transitions -- all 11 claude_only checks stay PRIMARY_CLAUDE (see enforcement-inventory.json's portability.legacy_hook_state field). A future cycle appends an entry here only once it carries the full promotion bundle (or a REMOVE + tombstone_ref) the schema requires; it must never delete this file's header or silently shrink an existing entries array."
}
