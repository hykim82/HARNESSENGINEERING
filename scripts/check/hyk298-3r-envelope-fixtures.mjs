// HYK-298-label-classify-3 (coder-task.md) -- 오늘 실제로 막힌
// 두 봉투(2R coder.md / 1R review.md)를 이 저장소 안 fixtures 모듈로
// 동결한다(절대경로를 시험 코드에 박지 마라, coder-task.md §3-1 원문).
// 원본은 hyk298-abort-record-r1-2 워크트리(무접촉 보존 대상, 쓰기/삭제
// 0)에 있고, 이 모듈은 그 파일들의 바이트를 JSON.stringify로
// 그대로 옮긴 사본이다 -- 손으로 다시 타이핑하지 않았다(타이핑
// 과정에서 백틱·인용부호가 섞인 실물 텍스트가 미묘하게 달라지는
// 사고를 피하기 위해). hyk298-3r-envelope-fixtures.test.mjs가
// 아래 SHA-256 두 값을 실물과 대조해, 이 동결본이 실제로 실물과
// 바이트 단위로 같음을 기계로 확인한다.

export const CODER_2R_ENVELOPE_SHA256 =
  "293c653d3942a678aeddb655836a525d2f62d91ccd5132910a76cf923248c47a";
export const REVIEW_1R_ENVELOPE_SHA256 =
  "45affa8c2a4b8b773fe6569d3fff2d36734b2540ca5879e3022cee645afad741";

// 15729 bytes -- 2R CODER 결과 봉투(.harness/coder.md) 원문 그대로.
export const CODER_2R_ENVELOPE_CONTENT =
  'dispatch_verified: yes\ntask_id_from_dispatch: task_8243e4ea45b9\npane_match: 2a3339ac-240b-4d52-8699-9b5b180c0b13:33f1eef9-6fd5-4b03-8831-c27ee9c6f242 == 2a3339ac-240b-4d52-8699-9b5b180c0b13:33f1eef9-6fd5-4b03-8831-c27ee9c6f242 ? 일치\nfor: HYK-298\nrole: CODER\ntask_id: HYK-298-abort-record-2\n\n# 1. 한 일 (요약, 1R 위에 이어서)\n\n1R의 반려 사유(★공통 문장: "이름표가 «없는 것»과 «깨진 것»은 다르다")를\n고쳤다. 1R은 `extractSoleMatch`가 0개 매치(진짜 없음)와 2개 이상 매치\n(깨짐)를 똑같이 `undefined`로 뭉개는 바람에, `task_id:` 가 **복수**·\n**공백/빈 값**·**줄 중간**·**줄넘김 오인식**으로 깨진 결과 파일에도\n중단 축이 열려 ALLOW가 났다(검토가 재현한 원문 그대로). 이번 라운드는\n결과 파일의 `task_id:` 를 3가지 상태(`MISSING`/`VALID`/`BROKEN`)로\n먼저 구조적으로 가르는 `classifyTaskIdLabel`을 추가하고, **BROKEN이면\n중단 기록 후보를 읽지도 않고**(§2-1 원문 "중단 기록이 아무리 완벽해도\n열리지 않는다") 그 자체로 fail-closed REJECT를 낸다.\n\n변경 파일 (1R 산출물 위에 이어서 수정, 되돌리지 않음):\n- `scripts/check/dispatch-gate-decision.mjs` -- `classifyTaskIdLabel`\n  (신규, MISSING/VALID/BROKEN 판별) + `labelInfoToHarnessTaskLabel`\n  (신규, 분리 헬퍼) + `rejectForBrokenLabel`(신규, BROKEN 전용 REJECT) +\n  `maybeResolveAbortRecordForMissingLabel` 시그니처를\n  `harnessTaskLabel` → `labelInfo` 로 교체(kind별 분기) + `CONSUMPTION_TASK_ID_RE_G`\n  자체를 `\\s*` → `[ \\t]*` 로 고쳐 크로스라인 오추출을 막음.\n- `scripts/check/dispatch-gate-abort-wire.test.mjs` -- §D 5개 시험(깨진\n  4형태 각각 + 회귀 대조군) + RED 변이 1개 + `withFixtureDir`가 콜백\n  반환값을 전달하도록 수정(§D-4가 gate 결과를 이어서 단언하는 데 필요,\n  기존 호출자들은 반환값을 안 썼으므로 회귀 없음).\n\n`consumption-receipt-core.mjs` -- **계속 무변경**(0줄 diff, 지시서\n§4 그대로). `admission-completion-adapter.mjs`·`selfcheck-smoke.mjs`\n-- **무접촉**(§4 그대로, 1R과 동일).\n\n# 2. 무엇을 어떻게 갈랐는가\n\n## 2-1. `classifyTaskIdLabel` -- 3개 정규식으로 삼분\n\n```js\nconst TASK_ID_ANY_RE = /task_id:/gi;               // 줄 무관, 파일 어디에나\nconst TASK_ID_LOOSE_LINE_RE = /^task_id:.*$/gim;    // 줄 시작, 값 유효성 무관\nconst CONSUMPTION_TASK_ID_RE_G = /^task_id:[ \\t]*(\\S+)/gim; // 줄 시작 + 같은 줄 값(고쳐짐)\n```\n\n- `anyCount === 0` → **MISSING**(진짜 없음, 1R §A 그대로 abort 축 진입).\n- `anyCount === looseLines === 1 && strictCount === 1` → **VALID**(정상,\n  axis 자체를 건드리지 않음 -- 1R §C 그대로).\n- 그 외 전부 → **BROKEN**(fail-closed REJECT, axis 진입 차단).\n\n`anyCount !== looseLines` 이면 "줄 시작이 아닌 곳에 task_id:가 있다"는\n뜻(ⓒ 줄 중간)이라 자동으로 BROKEN 분기로 떨어진다. `looseLines >= 2`\n(ⓐ 복수)나 `looseLines === 1 && strictCount === 0`(ⓑ 빈 값/ⓓ 크로스라인\n실패 매치)도 같은 else 분기로 떨어진다 -- 새 상태를 늘리지 않고 "정확히\n하나의 깨끗한 조건만 VALID, 그 외는 전부 fail-closed"라는 이 저장소의\n기존 관례(consumption-receipt-core.mjs §4 "판정 불가를 정상으로 접지\n않는다")를 그대로 따랐다.\n\n## 2-2. 줄넘김 원인(ⓒ) 수리 -- 범위와 안 바꾼 이유\n\n`CONSUMPTION_TASK_ID_RE_G`의 `\\s*`(줄바꿈도 먹음)를 `[ \\t]*`(수평\n공백만)로 고쳤다. 이 상수는 `findArchivedDroppedAt`/\n`findArchivedResultFingerprint`(아카이브 사본의 task_id 대조)에서도\n재사용되므로 그 두 자리도 함께 고쳐졌다 -- 셋 다 "같은 줄 안의 값만\n신뢰한다"는 같은 목적이라 갈라 둘 이유가 없고, 회귀 시험(§B/§C/§E\n전부·hyk263 전체)이 그대로 통과해 부작용이 없음을 확인했다.\n\n`CONSUMPTION_DONE_RE_G`(`>>> DONE:` 시각 추출)는 **건드리지 않았다** --\n이유: (a) 이 규칙은 doneAt 추출용으로, 이번 축(§2-1의 이름표 없음/깨짐\n판별)의 대상이 아니다. (b) `>>> DONE: ROLE @ <시각>` 형식은 애초에\n"@" 뒤 나머지 전부를 같은 줄에서 캡처하도록 설계돼 있어(`.*@\\s*(.+?)\\s*$`)\n크로스라인으로 다음 줄을 잘못 집어올 구조적 위험이 `task_id:`와 다르다\n(non-greedy `+?` + `$` 앵커가 이미 같은 줄로 값을 좁힌다). (c) 지시서\n§2-2가 명시적으로 "다른 축의 기존 동작을 깨뜨리지 마라"고 요구했다 --\n필요하지 않은 변경을 하지 않는 것이 그 요구를 만족하는 가장 안전한\n길이다.\n\n## 2-3. HYK-262와의 관계 (⛔확대 안 함)\n\n이 결함은 **HYK-262**(«표지 줄 유일성 계약 비대칭 -- 커밋 게이트는\n표지 2개를 막는데 소비 경로는 안 막는다»)와 **같은 계열**이다 --\n`task_id:` 표지 줄의 유일성이 소비(게이트) 경로에서 강제되지 않았던\n것이 이번 사고의 뿌리이기도 하다. 이 라운드는 **중단 축의 누수만**\n닫았고, HYK-262의 일반형(다른 표지 줄·다른 경로 전반)은 건드리지\n않았다 -- 별도 이슈로 남긴다.\n\n# 3. ★★합격 기준 4개 -- 실행 출력 원문\n\n명령: `node --import ./scripts/check/sweep-ledger-isolation.mjs --test scripts/check/dispatch-gate-abort-wire.test.mjs`\n\n## 3-1/3-2. 깨진 4형태 REJECT(각각 유효한 중단 기록을 붙인 채) + 진짜 없음 ALLOW\n\n```\n{"case":"multiple","result_status":1,"allow":false,"abort_axis":false}\n{"case":"whitespace_eof","result_status":1,"allow":false,"abort_axis":false}\n{"case":"middle_of_line","result_status":1,"allow":false,"abort_axis":false}\n{"case":"whitespace_crossline","result_status":1,"allow":false,"abort_axis":false}\n{"case":"missing_real","result_status":0,"allow":true,"abort_axis":true}\n```\n\n(검토가 낸 반려 원문 `{"case":"multiple","result_status":0,"allow":true,"abort_axis":true}`\n등과 같은 형식 -- 대조: 4형태 전부 `result_status`가 `0`에서 `1`로,\n`allow`가 `true`에서 `false`로, `abort_axis`가 `true`에서 `false`로\n뒤집혔다. `abort_axis`는 stderr에 `중단 기록(`이 실제로 찍혔는지로\n판별한다 -- BROKEN은 이 문구조차 없어야 한다는 뜻, 즉 후보를 읽지도\n않고 걸린다는 증거.)\n\n`whitespace_crossline`(ⓓ)의 REJECT 사유에는 `label=verdict:` 오추출\n흔적이 더 이상 없다(`assert.doesNotMatch(r.stderr, /label=verdict:/)`\n통과 -- ⓒ 원인 수리 확인).\n\n## 3-3. 전체 시험 요약 (전건 통과, §A/§B/§C/RED 회귀 0)\n\n```\n$ node --import ./scripts/check/sweep-ledger-isolation.mjs --test scripts/check/dispatch-gate-abort-wire.test.mjs\n...\nℹ tests 13\nℹ suites 0\nℹ pass 13\nℹ fail 0\nℹ cancelled 0\nℹ skipped 0\nℹ todo 0\n```\n\n1R의 §A(GREEN)·§A 대조군·§B-1/2/3·§C·RED(결선 제거 변이) 7개 시험\n전부 이번 수정 이후에도 그대로 통과 -- §D의 5개 신규 시험(깨진 4형태 +\n회귀 대조군) + RED(2R) 변이 1개가 추가돼 총 13개.\n\n## 3-4. RED 변이 (필수, 즉시 원복 확인)\n\n`maybeResolveAbortRecordForMissingLabel`에서 `if (labelInfo.kind ===\n"BROKEN") return rejectForBrokenLabel(labelInfo);` 한 줄을 제거하면\n(BROKEN을 다시 MISSING처럼 취급) §D-1(복수 라벨, 완전히 유효한 기록\n첨부)이 다시 `ALLOW`로 샌다 -- 위 3-3 출력의\n`✔ RED(변이, 필수, 2R): ... §D-1(복수 라벨)이 다시 ALLOW로 샌다`가 그\n증거(mutation 시험이 통과 = RED 재현이 실제로 확인됨). ⛔합성 mutant는\n격리 tmpdir(`stageScriptsCheckDir`)에만 적용했고, 원본\n`dispatch-gate-decision.mjs`는 그 즉시(같은 시험 함수 안에서) 원래\n소스로 대체 없이 그대로 유지된다 -- 실제 파일은 한 번도 변이되지\n않았다(문자열 치환은 메모리상의 사본에서만 일어난다).\n\n# 4. 검증 (§5 요구 그대로, 숫자는 전부 출력에서 복사)\n\n## 4-1. 개별 시험 (모두 통과)\n\n```\nabort-record-core.test.mjs             : tests 12, pass 12, fail 0\nabort-record-writer.test.mjs           : tests  8, pass  8, fail 0\ndispatch-gate-abort-wire.test.mjs      : tests 13, pass 13, fail 0 (위 §3 원문)\ndispatch-gate-consumption-wire.test.mjs: tests 18, pass 18, fail 0 (회귀 0)\nhyk241-oneb-gate-mutation.test.mjs     : tests  3, pass  3, fail 0 (회귀 0)\nhyk263-archive-doneat.test.mjs         : tests  8, pass  8, fail 0 (회귀 0)\ndispatch-gate-decision.test.mjs        : tests 38, pass 38, fail 0 (회귀 0)\n```\n\n## 4-2. CI 정본 (isolated-suite-runner, ⚠️1R 오기재의 진짜 원인을 여기서 발견·수정)\n\n**★1R(및 이번 라운드 첫 시도)이 `isolated-suite-runner.mjs`를\n포지셔널 인자로 불렀는데, 그 CLI는 `--repo-root <path>`만 받는다**\n(스크립트 148행 `if (args[i] === "--repo-root") sourceRoot = args[++i];`\n원문 확인). 포지셔널로 넘기면 `sourceRoot`가 조용히 `undefined`로\n남아 `runIsolatedSuite`가 `repoRootOf(process.cwd(), ...)`(=이 워크\n트리 자신, 커밋 0이므로 HEAD가 그대로 `c600bd8`)로 폴백한다 --\n스크래치 클론을 만들어 커밋까지 했어도 **그 스크래치 클론은 실제로는\n한 번도 테스트되지 않았다.** 잘못된 방식으로 먼저 돌렸을 때 배너가\n`tested commit c600bd872e3357531dc23a4c91dd644ac626894a`(=base, 내\n변경분 0개 반영)를 찍었고, `grep`으로 §D 시험 이름이 로그에 전혀 없다는\n것으로 직접 확인했다(스크래치 클론 파일 자체에는 §D가 있는데 로그에는\n없음 -- 실행된 트리가 다르다는 확실한 증거). `--repo-root`로 정정해\n재실행하니 배너가 실제 스크래치 커밋\n(`8025714db96a1b2d65c72eeca2986db742205693`)을 정확히 찍었고, 로그에\n§D 시험 5개 + RED(2R) 1개가 실제로 나타났다.\n\n```\n$ git clone --quiet . /c/.../hyk298-2r-ci-scratch\n$ (9개 변경/신규 파일 복사) git commit -m "scratch: ..."\n$ ADMISSION_LEDGER_PATH=/c/.../hyk298-2r-ci-isolated-ledger.json \\\n  ADMISSION_LOCK_PATH=/c/.../hyk298-2r-ci-isolated-ledger.lock \\\n  node scripts/check/isolated-suite-runner.mjs --repo-root /c/.../hyk298-2r-ci-scratch\n[isolated-suite-runner] tested commit 8025714db96a1b2d65c72eeca2986db742205693 -- ran against an isolated clone of committed HEAD only, uncommitted changes are NOT included in this run\n[isolated-suite-runner] clone: C:\\...\\hyk208-isolated-xrYNs1 (233 test file(s))\n...\nℹ tests 4547\nℹ suites 2\nℹ pass 4541\nℹ fail 0\nℹ cancelled 0\nℹ skipped 6\nℹ todo 0\n```\n\n(참고: 검토의 1R 재실행값 `tests 4541 / pass 4535 / fail 0 / skipped 6`\n대비 정확히 `+6/+6` -- §D 신규 시험 6개(13-7) 순증분과 일치, 숫자가\n서로 정합적이다.)\n\n⚠️**이것은 1R 결과 파일의 §4-2 수치도 무효였다는 뜻이다** -- 1R에서\n"tests 4514 / pass 4508"라고 적은 것은 실제로 **base commit(내 산출물\n0개 반영)을 테스트한 결과**였다(같은 잘못된 호출 방식). 코드 자체는\n1R 시점에도 개별 시험(격리 러너)과 전체 스윕에서 이미 실전 검증됐으므로\n1R의 실질적 결론(§A/§B/§C 전부 GREEN)은 바뀌지 않지만, "CI 정본"이라고\n표기했던 그 특정 수치는 사실 이 워크트리의 CI 정본 검증이 아니었다.\n2R부터는 위처럼 `--repo-root`로 바로잡았다.\n\n## 4-3. 전체 스윕 (`node scripts/check/full-sweep-local.mjs`)\n\n```\nℹ tests 4547\nℹ suites 2\nℹ pass 4544\nℹ fail 0\nℹ cancelled 0\nℹ skipped 3\nℹ todo 0\n```\n\n## 4-4. 관제실 원장 2파일 SHA-256 (전/후 동일, 이번 라운드 기준)\n\n```\n전: 1d05feac156f266128105b46d7cf092bc16323fbf73d26371f6ee9cb20069c49  admission-ledger.json\n    c28ebdb2070807ba9be4e0decfb8cf4c9b660b42f45a6ffd7b6264b9f4982056  admission-ledger.json.completion-failures.jsonl\n후: 1d05feac156f266128105b46d7cf092bc16323fbf73d26371f6ee9cb20069c49  admission-ledger.json\n    c28ebdb2070807ba9be4e0decfb8cf4c9b660b42f45a6ffd7b6264b9f4982056  admission-ledger.json.completion-failures.jsonl\n```\n(1R 결과의 전/후 값과 절대값이 다른 것은 그 사이 실제 관제실 운영으로\n원장이 자연 변화했기 때문 -- 이 라운드 자신의 작업 전/후는 바이트\n동일, 내가 원장에 닿지 않았다는 증거는 이 두 줄이다.)\n\n## 4-5. eslint / prettier (변경 파일 전부)\n\n```\n$ npx eslint scripts/check/dispatch-gate-decision.mjs scripts/check/dispatch-gate-abort-wire.test.mjs\n(무출력 -- 0 문제)\n$ npx prettier --check scripts/check/dispatch-gate-decision.mjs scripts/check/dispatch-gate-abort-wire.test.mjs\nChecking formatting...\nAll matched files use Prettier code style!\n```\neslint-disable·규칙 완화·설정 변경 0. `evaluateConsumptionDecision`\n자신은 이번에 새 분기를 추가하지 않았다(§2 진입 조건 판정은 여전히\n`maybeResolveAbortRecordForMissingLabel` 안에 있고, `classifyTaskIdLabel`\n결과를 소비하는 부분만 `labelInfoToHarnessTaskLabel`로 한 번 더\n쪼갰다) -- complexity 경고 재발 없음.\n\n## 4-6. `git status --short` (커밋 0)\n\n```\n M scripts/check/dispatch-gate-consumption-wire.test.mjs\n M scripts/check/dispatch-gate-decision.mjs\n M scripts/check/hyk241-oneb-gate-mutation.test.mjs\n M scripts/check/hyk263-archive-doneat.test.mjs\n?? scripts/check/abort-record-core.mjs\n?? scripts/check/abort-record-core.test.mjs\n?? scripts/check/abort-record-writer.mjs\n?? scripts/check/abort-record-writer.test.mjs\n?? scripts/check/dispatch-gate-abort-wire.test.mjs\n```\n(1R과 파일 목록 동일 -- 이번 라운드는 기존 9개 파일 중 2개, 즉\n`dispatch-gate-decision.mjs`·`dispatch-gate-abort-wire.test.mjs`만\n추가로 손댔고 새 파일은 만들지 않았다.) `git log -1`은 여전히\n`c600bd8` -- 커밋 0, push 0, PR 0, rebase 0.\n\n# 5. ⛔정직 한계 (무엇을 여전히 못 막는가)\n\n1. **`classifyTaskIdLabel`은 여전히 정규식 기반 텍스트 판별이다** --\n   `task_id:`라는 문자열이 값 안에(예: 다른 필드의 자유 텍스트 안에)\n   우연히 등장하면 그 라운드는 BROKEN으로 오분류될 수 있다(더 정밀한\n   구조화 파서가 아니라면 이 클래스의 오탐은 원리적으로 남는다) --\n   그러나 이 방향의 오분류는 **항상 안전측(REJECT)**이라 §0의 실사고\n   (조용한 ALLOW)와는 다른 위험 등급이다.\n2. **HYK-262(표지 줄 유일성 계약 비대칭)의 일반형은 그대로 남아\n   있다** -- 이 라운드는 `task_id:` 하나(중단 축의 진입 조건)만\n   고쳤을 뿐, 결과 파일의 다른 표지 줄(`for:`/`role:`/`>>> DONE:` 등)\n   이 소비 경로에서 같은 종류의 "복수/깨짐" 취약점을 갖는지는 확인하지\n   않았다(⛔범위 밖, §2-3).\n3. **여전히 정직 한계로 남는 것(1R과 동일)**: 이 축은 dispatch-\n   receipts.jsonl·admission 원장 자체의 무결성(누가 썼는지, 변조\n   여부)은 보장하지 않는다 -- 그 두 파일의 자기 보호는 이 축의 범위\n   밖이다.\n4. **재현하지 못한 것**: 실제 관제실 쪽이 이번에 고친 판별 로직을\n   거쳐 실제로 깨진 이름표를 만들어내는 시나리오(예: 워커 프로세스가\n   정확히 이 4형태 중 하나로 죽는 실물 재현)는 이 워크트리 범위 밖 --\n   이번 시험은 검토가 재현한 원문과 같은 모양을 합성해 검증했다.\n\n# 6. 완료 표지\n\n명령: `node scripts/relay/finalize-done.mjs coder .harness`\n출력: `FINALIZED: >>> DONE: CODER @ 2026-08-18 13:06:00 KST`\n>>> DONE: CODER @ 2026-08-18 13:06:00 KST\n';

// 4474 bytes -- 1R REVIEW 결과 봉투(.harness/review.md) 원문 그대로.
export const REVIEW_1R_ENVELOPE_CONTENT =
  'dispatch_verified: yes\ntask_id_from_dispatch: task_47e490fff01f\npane_match: fc6a65e1-f014-4d89-8e78-366f118645ac:56b9263c-d60f-453e-84d4-625cef2ef396 == fc6a65e1-f014-4d89-8e78-366f118645ac:56b9263c-d60f-453e-84d4-625cef2ef396 ? 일치\nfor: HYK-298\nrole: REVIEW\ntask_id: HYK-298-abort-record-review-1\nverdict: rejected\n\n# 판정\n\nREJECT. CI 정본과 세 상태 분리는 통과했지만, 깨진 `task_id:` 경계에서 중단 축이 새는 결함을 직접 재현했다.\n\n## 핵심 반려 사유\n\n`extractSoleMatch()`는 `task_id:` 줄이 0개·복수개이면 `undefined`를 반환하고, `maybeResolveAbortRecordForMissingLabel()`는 이를 모두 “이름표 없음”으로 취급해 중단 기록 축을 연다. 따라서 유효한 중단 기록을 붙인 다음 입력이 모두 ALLOW됐다.\n\n```text\n{"case":"multiple","result_status":0,"allow":true,"abort_axis":true}\n{"case":"whitespace_eof","result_status":0,"allow":true,"abort_axis":true}\n{"case":"middle_of_line","result_status":0,"allow":true,"abort_axis":true}\n```\n\n이는 “이름표가 실제로 없는 경우에만”이라는 핵심 조건을 깨며, 복수·공백·줄 중간의 깨진 이름표를 안전측 REJECT로 닫지 않는다. 공백 뒤 다음 줄을 `task_id` 값으로 잘못 주워오는 별도 오인식도 확인했다(`whitespace_crossline`에서 `label=verdict:`).\n\n## 직접 검증 원문\n\ndispatch 대조:\n\n```text\ndispatch-show task_47e490fff01f: assignee_pane_key = fc6a65e1-f014-4d89-8e78-366f118645ac:56b9263c-d60f-453e-84d4-625cef2ef396\nORCA_PANE_KEY = fc6a65e1-f014-4d89-8e78-366f118645ac:56b9263c-d60f-453e-84d4-625cef2ef396\n```\n\n핵심 wire 시험:\n\n```text\n✔ §A GREEN: 이름표 없이 죽은 라운드 + 검증 가능한 중단 기록(지문·dispatchId·회수표식 셋 다 실물) -> 다음 배달 ALLOW\n✔ §A 대조군(회귀 0): 중단 기록을 아예 안 남기면 원래 사고와 똑같이 REJECT\n✔ §B-1 위조(지문 불일치) -> REJECT\n✔ §B-2 위조(dispatchId 불명) -> REJECT\n✔ §B-3 위조(회수 표식 없음) -> REJECT\n✔ §C 진짜 미소비 여전히 차단 -> REJECT 유지\n✔ RED(변이, 필수): abortOutcome.done 단락 제거 -> §A ALLOW 소실\nℹ tests 7\nℹ pass 7\nℹ fail 0\n```\n\nCI 정본 재실행(9개 변경·신규 파일을 스크래치 커밋 `7a6614f`에 포함):\n\n```text\n[isolated-suite-runner] tested commit 7a6614f7cd1bb1026908190cf70dcb8bcf6f8661 -- ran against an isolated clone of committed HEAD only, uncommitted changes are NOT included in this run\n[isolated-suite-runner] clone: C:\\Users\\Administrator\\AppData\\Local\\Temp\\hyk208-isolated-sgjJaM (233 test file(s))\nℹ tests 4541\nℹ pass 4535\nℹ fail 0\nℹ skipped 6\nrunner_exit=0\n```\n\n따라서 작업자의 4514 기재는 틀렸고, 재실행한 CI 정본은 신규 27건을 포함했다(전체 4541). live 전체 스윕도 `ℹ tests 4541 / ℹ pass 4538 / ℹ fail 0 / ℹ skipped 3 / full_sweep_exit=0`이었다.\n\n## 범위·품질 점검\n\n- 개별 격리 시험 11개를 모두 지정된 `node --import ./scripts/check/sweep-ledger-isolation.mjs --test …` 형식으로 실행했고 종료 코드는 전부 0이다.\n- ESLint exit 0, Prettier `All matched files use Prettier code style!`, `git diff --check` exit 0.\n- `consumption-receipt-core.mjs` diff 0, C 레인 `admission-completion-adapter.mjs`·`selfcheck-smoke.mjs` diff 0.\n- git status는 변경 4개 + 신규 5개뿐이고 HEAD는 `c600bd8` 그대로다. 코드 수정·커밋·push·PR·Linear 호출은 하지 않았다.\n- 관제실 원장 지문은 실행 전후 동일했다: `admission-ledger.json` = `AC7DFA645A8EE15318B0FA224805766A327DA218474442C114A80ECFA9A8D997`, `admission-ledger.json.completion-failures.jsonl` = `C28EBDB2070807BA9BE4E0DECFB8CF4C9B660B42F45A6FFD7B6264B9F4982056`. task 파일에 적힌 참고 지문과 현재 실제 지문은 달랐지만, 이번 실행 전후 불변은 확인했다.\n\n설계상 `consumption-receipt-core.mjs` 무변경은 zero-import 판정 코어와 어댑터의 사실 추출 분리라는 기존 구조상 타당하다. 정직 한계로 기록된 “중단 기록 작성 주체와 영수증·원장 신뢰성은 이 축이 인증하지 않는다”도 확인했지만, 그 한계와 별개로 위 malformed-label 누수는 반드시 고쳐야 한다.\n\n완료 표지 명령: `node scripts/relay/finalize-done.mjs review .harness`\n\n검토 시각(date): 2026년 8월 18일 화요일 오후 12:47:55\n>>> DONE: REVIEW @ 2026-08-18 12:48:50 KST\n';
