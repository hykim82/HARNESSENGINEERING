# 위임 패킷: <제목>

<!-- PM→ORCH 인계 계약. PRD 전체가 아니라 "이번 릴레이 페이즈 스코프"만 담는다.
     ORCH는 아래 승인란에 사람 서명이 없으면 이 패킷을 소비하지 않는다 —
     `scripts/check/packet-gate.mjs`가 서명 형식을 기계 검사한다(이 패킷이
     `*-task.md` 파일의 `packet:` 줄에 인용되는 순간 role-guard가 자동으로
     호출한다 — 별도 수동 실행 불필요, 단 ORCH는 소비 전에 직접 한 번 더
     `node scripts/check/packet-gate.mjs <이 패킷 경로>`로 확인하는 것을 권장).
     이 파일은 참고용 골격이다 — 실제 사본은 각 프로젝트의 PM 레인(관례:
     control-room-path 아래 PM 폴더)에 배치해 쓴다. -->

```
packet_id: PKT-YYYYMMDD-<slug>
from: PM(<모델명>) · to: ORCH
track: <이 프로젝트의 트랙/영역 이름>
prd: <PRD 경로 (없으면 "없음 — 단독 패킷")>
작성: YYYY-MM-DD HH:MM KST
승인: ☐ (미서명 상태 — ORCH 소비 금지. 사람이 서명할 땐 이 줄 전체를 `승인: OK <이름> YYYY-MM-DD HH:MM` 로 교체 — packet-gate가 검사하는 정확한 형식)
```

## 1. 문제 한 줄
- 

## 2. 이번 페이즈 스코프
- 포함:
- **제외(명시)**:

## 3. 완료 기준 (검증 가능 — ORCH가 이 프로젝트의 게이트 카드에 병합)
| ID | 기준(검증 가능 문장) | 검사 방법 |
|---|---|---|
| G1 | | |

## 4. 제약 · 리스크
- (예: 팀 repo에 하네스 흔적 금지, 기존 기능 회귀 금지=스모크 테스트 PASS 유지)

## 5. 분해 제안 (힌트 — 최종권 ORCH)
- 사이클 1: … / 사이클 2: … (의존 관계만)

## 6. 열린 질문 (실행 중 걸리면 PM 인박스로 되돌릴 것)
- 

## 한계 (필수)
- 이 패킷이 보장 못 하는 것:

<!-- ORCH 소비 기록(ORCH가 기입): consumed @ 시각 · Linear <이슈> · 게이트 병합 <항목ID들> -->
